import React from 'react';

const width = window.innerWidth

function AuthForm() {
    return (
        <div className='AuthForm' style={{width: width - 40, marginLeft: 20}}>
            
        </div>
    );
}

export default AuthForm;